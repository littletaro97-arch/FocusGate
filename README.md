# FocusGate / SearchGate

当前上传版本：`v0.5.2_20260708_beta_polish`

这是检索门 Android 众测候选版源码。仓库只保留最新工程，不包含历史 `versions` 归档和本地迭代备份。

## Build

```powershell
.\gradlew.bat assembleDebug
.\gradlew.bat assembleRelease
```

当前构建结果见 `DEBUG_REPORT.md` 和 `CURRENT_STATUS.md`。

## APK

最新众测 APK 位于：

```text
release/v0.5.2/
```

- `focusgate-v0.5.2-beta-debug.apk`
- `focusgate-v0.5.2-beta-release-unsigned.apk`

注意：`release-unsigned` 未签名，正式发给普通用户前需要签名。
